export class Config {
  app_base_url: string;
  header_json: string;
  aboutMe_json: string;
  recommendation_json: string;
  projects_json: string;
  experience_json: string;
  education_json: string;
  certificates_json: string;
  referee_json: string;
  external_resources_json: string;
  required: { name: string, value: boolean }[];
}
